apiVersion: messaging.platform.ohanyere.io/v1alpha1
kind: SQSQueueClaim
metadata:
  name: workflow-debug-api-queue
  labels:
    app.kubernetes.io/name: workflow-debug-api
    finops.ohanyere.io/cost-center: developer-experience
spec:
  environment: dev
  visibilityTimeoutSeconds: 30
  messageRetentionSeconds: 345600
  deadLetterQueue:
    enabled: true
    maxReceiveCount: 5
